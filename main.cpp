#include "Controlador.h"

int main() {
    Controlador controlador; // el Controlador carga los datos de prueba en su constructor
    controlador.iniciar();   // main.cpp SOLO instancia y llama iniciar() (HU04)
    return 0;
}
